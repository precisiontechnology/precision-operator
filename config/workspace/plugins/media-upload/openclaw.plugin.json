{
  "id": "media-upload",
  "name": "Media Upload (R2)",
  "description": "Intercepts outbound messages, uploads local file paths to Cloudflare R2, and rewrites URLs before delivery.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
