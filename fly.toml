app = "precision-operator-test"
primary_region = "ewr"

[build]

[mounts]
  source = "openclaw_data"
  destination = "/home/node/.openclaw"

[env]
  # Per-user injection (optional)
  # PRECISION_USER_NAME = "Test Account"
  # PRECISION_USER_CONTEXT = "Testing deployment"